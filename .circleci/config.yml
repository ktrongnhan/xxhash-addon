version: 2.1

commands:
  parameterized_steps:
    parameters:
      compiler:
        default: "g++"
        type: string
    steps:
      - checkout
      - run: git submodule update --init
      - run: sudo apt-get install python <<parameters.compiler>> make
      - run: |
          node --version
          npm --version
          <<parameters.compiler>> --version

      # Download and cache dependencies
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            # fallback to using the latest cache if no exact match is found
            - v1-dependencies-

      - run: npm run debug:install
      - run: npm install jest --save-dev

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

      - run: LD_PRELOAD=$(gcc -print-file-name=libasan.so) LSAN_OPTIONS=suppressions=suppr.lsan node node_modules/jest/bin/jest.js
      - run: LD_PRELOAD=$(gcc -print-file-name=libasan.so) LSAN_OPTIONS=suppressions=suppr.lsan node benchmark.js

      # package and install
      - run: |
          npm run ci:install
          npm pack
          mkdir ~/package_install
          cp xxhash-addon*.tgz ~/package_install
          cd ~/package_install
          npm install xxhash-addon*.tgz

jobs:
  nodelatest-gcc:
    docker:
      - image: cimg/node:current

    working_directory: ~/xxhash-addon

    steps:      
      - parameterized_steps:
          compiler: "g++"

  nodelatest-clang:
    docker:
      - image: cimg/node:current

    working_directory: ~/xxhash-addon

    steps:      
      - parameterized_steps:
          compiler: "clang"

  node8-gcc:
    docker:
      # specify the version you desire here
      - image: cimg/node:8.6

    working_directory: ~/xxhash-addon

    steps:      
      - parameterized_steps:
          compiler: "clang"

  node8-clang:
    docker:
      # specify the version you desire here
      - image: cimg/node:8.6

    working_directory: ~/xxhash-addon

    steps:      
      - parameterized_steps:
          compiler: "g++"

workflows:
  version: 2
  build_all:
    jobs:
      - nodelatest-gcc:
          filters:
            branches:
              only:
                - master
                - v2
      - nodelatest-clang:
          requires:
            - nodelatest-gcc
          filters:
            branches:
              only:
                - master
                - v2
      - node8-gcc:
          requires:
            - nodelatest-clang
          filters:
            branches:
              only:
                - master
                - v2
      - node8-clang:
          requires:
            - node8-gcc
          filters:
            branches:
              only:
                - master
                - v2